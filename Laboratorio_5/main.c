#include <stdio.h>
#include "double_list.h"

// Definimos algunos valores a usar.
    #define x 190 
    #define y 25 
    #define z 223 

int main() {
    // Inicializamos nuestra lista.
    struct DoubleList lista ;
    inicializar_lista(&lista) ;

    // Insertamos algunos elementos en la primera posición.
    nodo_al_inicio(&lista, x) ;
    nodo_al_inicio(&lista, y) ;
        // Imprimimos la lista actual y agremos otro de primero.
        


}